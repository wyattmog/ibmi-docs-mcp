"""Test package for ibmi_docs_mcp."""

# TODO: add pytest config (pyproject or conftest) for src layout + fixtures path helper.
# Why: `uv run pytest` should resolve `ibmi_docs_mcp` and `tests/fixtures/` without hacks.
