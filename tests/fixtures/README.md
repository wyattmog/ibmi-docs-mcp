# Fixture files (SPEC §11)

Captured from IBM Documentation public APIs for offline tests.

| File | Purpose |
|------|---------|
| `search_http_get.json` | Happy-path search envelope + topic fields |
| `search_wrkactjob.json` | Multi-hit / pagination shape |
| `search_empty.json` | `hits:0`, `topics:[]` |
| `content_http_get.html` | Topic HTML with `main`/`article`/`h1` |
